#include <iostream>
using namespace std;
class student{
    public:
    static int count;
    student(){
        count++;
    }
    
    static void printCount(){
        cout << "Total count of Student is :" << student::count;
    }
};

int student :: count = 0;

int main(){ 
    student s1,s2,s3,s4;
    
    student::printCount();
    return 0;
}
